## Simple OpenGL test
A simple program to create a window with OpenGL on Linux. No pre-requisites required.

## Installation

```
cmake -B build
cmake --build build
```

## Execute
```
cd build
./main
```
